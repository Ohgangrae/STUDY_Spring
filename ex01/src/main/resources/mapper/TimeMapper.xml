<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ex01.mapper.TimeMapper">
    <select id="getTime" resultType="string">/*아이디를 TimeMapper인터페이스의 메소드의 이름으로 적어주면 끝*/
        SELECT SYSDATE FROM DUAL /*resultType은 getTime 메소드의 리턴타입과 동일하게 써야한다*/
        /*앞글자 소문자로 해야한다 string*/
    </select>
</mapper>